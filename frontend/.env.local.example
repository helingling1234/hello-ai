# URL of the FastAPI backend (used by next.config.ts rewrites)
# In development (running `npm run dev`): leave as default below
# In Docker: set to http://backend:8000 via docker-compose environment
BACKEND_URL=http://localhost:8000
